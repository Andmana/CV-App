const SkillEdit = ({
    sectionData,
    handleSectionDataChange,
    isFormOpen,
    handleFormOpen,
}) => {
    return (
        <>
            <h1 className="edit-section-header">Skills</h1>
        </>
    );
};

export default SkillEdit;
